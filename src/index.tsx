import React from 'react';
import ReactDOM from 'react-dom';

import { DemoMosaic } from './demo';

ReactDOM.render(
    <React.StrictMode>
        <DemoMosaic />
    </React.StrictMode>,
    document.getElementById('root')
);
